{
    "builds": [
        {
            "src":"app.py",
            "use":"@vercel/python"
        }
    ],
    "routes": [
        {
            "src":"/(.*)", 
            "dest":"app.py"
        }
    ],
    "includeFiles": ["templates", "requirements.txt", "weather_controller.py"]
}